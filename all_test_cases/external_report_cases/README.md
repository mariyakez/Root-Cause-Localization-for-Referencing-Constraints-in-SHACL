# External Report Compatibility Cases

These cases contain external validation reports that can be passed with
`--report`. They are designed to exercise pySHACL-style reports with
`sh:detail`, Jena-style reports without `sh:detail`, TopBraid-like reports, and
reports with missing metadata.
