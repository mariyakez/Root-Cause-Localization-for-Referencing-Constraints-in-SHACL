# Root-Cause-Localization-for-Referencing-Constraints-in-SHACL
Build a tool that takes SHACL validation results involving referencing constraints (primarily sh:node ) and produces an explanation tree that reveals the full set of concrete violations (“leaf failures”) inside referenced shapes.
